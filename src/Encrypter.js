

class Encrypter {
  /**
   * 
   * @param {string} key 
   */
  constructor (key) {
    this.key = key
    this.keyLength = key.length;
  }

  /**
   * Encrypts clearText to cipher text using a Vinegere cipher.
   * @param {string} clearText 
   */
  encrypt (clearText) {

  }
}